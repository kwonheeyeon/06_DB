-- MEMBERS 테이블 생성

CREATE TABLE MEMBERS(

MEMBER_ID VARCHAR2(20),

MEMBER_PWD VARCHAR2(20) NOT NULL,

--MEMBER_NAME CHAR(30),
MEMBER_NAME VARCHAR2(30),

MEMBER_AGE NUMBER,

MEMBER_EMAIL VARCHAR2(50),

PRIMARY KEY(MEMBER_ID)

);

INSERT INTO MEMBERS VALUES('ID', 'PW', 'NAME', 22, '');

SELECT * FROM MEMBERS;

DROP TABLE MEMBERS;

DROP TABLE BOARDS;

-- BOARDS 테이블 생성

CREATE TABLE BOARDS(

BOARD_NO NUMBER PRIMARY KEY,

BOARD_TITLE VARCHAR2(100),

BOARD_CONTENT VARCHAR2(4000),

--BOARD_WRITER VARCHAR2(20) FOREIGN KEY MEMBERS(MEMBER_ID),
BOARD_WRITER VARCHAR(20) REFERENCES MEMBERS(MEMBER_ID),

 BOARD_REG_DATE DATE DEFAULT CURRENT_DATE
--BOARD_REG_DATE DATE DEFAULT TO_DATE(CURRENT_DATE, 'YYYY-MM-DD')

);

INSERT INTO BOARDS VALUES(03, 'BB', 'CC', 'ID', DEFAULT);

SELECT * FROM BOARDS;












